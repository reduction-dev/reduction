syntax = "proto3";

package connectors.kafka.proto.v1;
option go_package = "connectors/kafkapb";

message Checkpoint {
  repeated Topic topics = 1;
}

message Topic {
  string name = 1;
  repeated Partition partitions = 2;
}

message Partition {
  int32 id = 1;
  int64 offset = 2;
}
